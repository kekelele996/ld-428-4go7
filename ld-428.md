<!-- 改编自：研究装修艺术主题项目提示词/ld-428/ld-428.md；后端由 NestJS + TypeScript（Mongoose）改造为 Go 1.22 + Gin + MongoDB（官方 mongo-driver） -->

# ld-428 艺术作品集与画廊管理平台

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**艺术作品集与画廊管理平台** — 面向独立艺术家和小型画廊，支持作品管理、线上展览策划、观众互动（点赞/评论/收藏）和艺术家个人主页。**本项目用于验证模型跨文件协同改动能力，禁止将多个职责合并进单一文件。** 项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **Artwork（作品）**
  - 字段：作品标题、作品描述、创作年份、媒介（OilPainting/Watercolor/Acrylic/Ink/Sculpture/Photography/Digital/Mixed/Installation/Other）、材质、尺寸（长×宽×高cm）、图片URL列表、视频URL（可选）、标签列表、艺术家ID、所属展览ID列表、作品状态（Draft/Published/Sold/Archived）、价格（可选）、浏览数、点赞数、收藏数
  - 关键关系：隶属于 Artist，可被多个 Exhibition 收录，被 Interaction 引用
  - 业务动作：上传作品、编辑信息、发布/下架
  - **此实体必须贯穿：MongoDB collection → backend/internal/model/artwork.go → backend/internal/repository/artwork_repository.go → backend/internal/service/artwork_service.go → backend/internal/handler/artwork_handler.go → backend/internal/router/artworks.go → frontend/src/api/artwork.ts → frontend/src/stores/artworkStore.ts → frontend/src/pages/Studio.tsx**

- **Exhibition（展览）**
  - 字段：展览标题、展览描述、策展人ID、开始日期、结束日期、展览类型（Solo/Group/Thematic/Permanent）、展览封面URL、作品ID列表、展览状态（Planning/Active/Ended/Archived）、参观人数
  - 关键关系：策展人（Curator）创建，包含多个 Artwork
  - 业务动作：创建展览、添加/移除作品、发布展览
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **Artist（艺术家）**
  - 字段：用户ID、艺术家名、个人简介、头像URL、擅长媒介列表、代表作品URL列表、社交媒体链接、关注者数、粉丝数、状态（Active/Inactive）
  - 关键关系：1个艺术家 → 多个作品
  - 业务动作：完善主页、上传代表作品、参与展览
  - **此实体必须贯穿全栈**

- **Interaction（互动记录）**
  - 字段：用户ID、目标类型（Artwork/Exhibition）、目标ID、互动类型（Like/Comment/Bookmark/Share）、评论内容（评论时有）、创建时间
  - 关键关系：被 Artwork/Exhibition 引用，维护点赞/收藏/评论计数
  - 业务动作：点赞、评论、收藏、取消
  - **此实体必须贯穿全栈**

- **ReviewLog（审核日志）**
  - 字段：目标类型（Artwork/Exhibition）、目标ID、审核人ID、审核结果（Approved/Rejected/Flagged）、审核意见、审核时间
  - 关键关系：记录 Artwork/Exhibition 的内容审核结果
  - 业务动作：提交审核、审核通过/驳回/标记
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/gallery（画廊首页）**
  - 消费实体：Artwork、Exhibition
  - 关键交互：精选展览轮播、最新作品瀑布流、热门作品排行
  - 共享组件：`<ArtworkCard>`、`<ExhibitionBanner>`、`<EmptyState>`

- **/artwork/:id（作品详情）**
  - 消费实体：Artwork、Interaction、Artist
  - 关键交互：大图浏览（支持缩放）、作品信息面板、点赞/收藏按钮、评论列表
  - 共享组件：`<ArtworkCard>`、`<CommentSection>`、`<InteractionBar>`

- **/exhibition/:id（展览详情）**
  - 消费实体：Exhibition、Artwork、Artist
  - 关键交互：展览封面大图、展览描述、作品网格列表、策展人信息
  - 共享组件：`<ArtworkCard>`、`<ExhibitionBanner>`

- **/artist/:id（艺术家主页）**
  - 消费实体：Artist、Artwork、Exhibition
  - 关键交互：艺术家头像和简介、代表作品轮播、作品集列表、参与展览
  - 共享组件：`<ArtworkCard>`、`<StatusBadge>`、`<UserAvatar>`

- **/studio（创作工作台，艺术家后台）**
  - 消费实体：Artwork、Exhibition
  - 关键交互：我的作品管理（草稿/已发布/已售Tab）、上传新作品、创建展览、数据统计（浏览/点赞/收藏趋势）
  - 共享组件：`<StatusBadge>`、`<StatCard>`、`<EmptyState>`

### 4. 横切关注点（≥ 2 个）

- **RBAC 权限控制**
  - 角色：Admin / Curator / Artist / Viewer
  - 触达文件层：
    - MongoDB：`users` 集合（含角色字段）、`roles` 集合
    - 后端：`backend/internal/middleware/auth.go`、`backend/internal/middleware/rbac.go`、`backend/internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中部分路由需 `AuthRequired()` 和 `RequireRole()`（Viewer 只能浏览和互动）
    - 前端：`router/guards.tsx`、`stores/authStore.ts`、`components/common/PermissionButton.tsx`
    - 类型：`frontend/src/types/enums.ts`

- **内容审核（Content Review）**
  - 艺术家提交的作品和展览需要审核后才公开
  - 触达文件层：
    - MongoDB：`review_logs` 集合
    - 后端：`backend/internal/middleware/content_review.go`、`backend/internal/service/review_service.go`、`backend/internal/model/review_log.go`
    - 前端：`pages/ReviewCenter.tsx`（管理员/策展人可见）、`types/enums.ts`

- **操作日志（Audit Log）**
  - 触达文件层：
    - MongoDB：`audit_logs` 集合
    - 后端：`backend/internal/middleware/audit_logger.go`、`backend/internal/service/audit_log_service.go`、`backend/internal/model/audit_log.go`
    - 前端：`api/audit.ts`、`pages/AuditLog.tsx`（管理员可见）

### 5. 共享枚举/常量（≥ 2 组）

- **ArtworkStatus（作品状态）**
  - 值：Draft / Published / Sold / Archived
  - 前端定义位置：`frontend/src/types/enums.ts`
  - 后端定义位置：`backend/internal/constants/artwork.go`
  - 出现位置：Artwork 模型、创作工作台 Tab、StatusBadge、作品审核流程
  - **README 必须列出该枚举在前后端的所有出现位置**

- **Medium（媒介类型）**
  - 值：OilPainting / Watercolor / Acrylic / Ink / Sculpture / Photography / Digital / Mixed / Installation / Other
  - 前端定义位置：`frontend/src/constants/mediumOptions.ts`
  - 后端定义位置：`backend/internal/constants/artwork.go`
  - 出现位置：Artwork 模型、上传作品表单、作品筛选器、艺术家擅长媒介

- **ExhibitionStatus（展览状态）**
  - 值：Planning / Active / Ended / Archived
  - 前端定义位置：`frontend/src/types/enums.ts`
  - 后端定义位置：`backend/internal/constants/exhibition.go`
  - 出现位置：Exhibition 模型、展览管理页、StatusBadge

- **InteractionType（互动类型）**
  - 值：Like / Comment / Bookmark / Share
  - 前端定义位置：`frontend/src/types/enums.ts`
  - 后端定义位置：`backend/internal/constants/interaction.go`
  - 出现位置：Interaction 模型、InteractionBar、统计接口

### 6. 共享前端组件清单（≥ 3 个）

1. **`<ArtworkCard>`** — 画廊首页、展览详情、艺术家主页、创作工作台
2. **`<ExhibitionBanner>`** — 画廊首页、展览详情
3. **`<CommentSection>`** — 作品详情
4. **`<InteractionBar>`** — 作品详情（点赞/收藏/分享按钮组）
5. **`<UserAvatar>`** — 作品详情、艺术家主页

### 7. 共享 hooks/utils 清单（≥ 2 个）

1. **`useInteraction`** — 封装点赞/收藏/评论的乐观更新逻辑
2. **`usePagination`** — 通用分页（作品列表用无限滚动）
3. **`formatArtworkSize`** — 尺寸格式化工具

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户 roles 校验权限（Viewer 只能浏览和互动）
- `audit_logger.go`：记录写操作日志
- `content_review.go`：内容审核拦截（未审核内容不公开返回）
- `error_handler.go`：统一错误响应格式
- `request_logger.go`：请求日志（request_id/method/path/status/latency_ms）

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`artvault`
- **前端端口**：18808 / 80
- **后端端口**：19308 / 8080（容器内部端口 8080）
- **前端框架**：React 18 + TypeScript + Vite
- **UI 库**：Tailwind CSS + Headless UI
- **图表**：ECharts（趋势图）
- **图片懒加载**：react-lazyload
- **状态管理**：Zustand
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin（`github.com/gin-gonic/gin`）
- **数据库**：MongoDB 7（文档型更适合艺术品管理）
- **数据访问**：官方 `go.mongodb.org/mongo-driver` + repository 模式（GORM 不适用于 MongoDB，repository 层用 mongo-driver 实现）
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`，密码哈希 `golang.org/x/crypto/bcrypt`）
- **文件上传**：本地存储或 MinIO（`github.com/minio/minio-go/v7`，用于作品图片/视频、展览封面、头像上传）
- **其他依赖**：`github.com/gin-contrib/cors`、`github.com/go-playground/validator/v10`、`log/slog`、`github.com/swaggo/gin-swagger`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # artwork.ts, exhibition.ts, artist.ts, interaction.ts
├── stores/           # artworkStore.ts, exhibitionStore.ts, artistStore.ts, interactionStore.ts
├── types/            # index.ts, enums.ts, artwork.ts, exhibition.ts, artist.ts, interaction.ts
├── components/common/  # ArtworkCard, ExhibitionBanner, CommentSection, InteractionBar, UserAvatar, EmptyState
├── hooks/            # useInteraction.ts, usePagination.ts
├── pages/            # Gallery, ArtworkDetail, ExhibitionDetail, ArtistProfile, Studio
├── router/           # index.tsx, guards.tsx
├── utils/            # formatArtworkSize.ts, request.ts
└── constants/        # apiPaths.ts, mediumOptions.ts
```

后端必须包含（Go 标准布局，repository 层使用 mongo-driver）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 MongoDB、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（MongoDB/JWT/端口/MinIO 配置）
    ├── model/                   # 按实体分文件：user.go, artwork.go, exhibition.go, artist.go, interaction.go, review_log.go, audit_log.go
    ├── repository/              # 按实体分文件（mongo-driver 实现）：user_repository.go, artwork_repository.go, exhibition_repository.go, artist_repository.go, interaction_repository.go, review_repository.go, audit_log_repository.go
    ├── service/                 # 按实体分文件：auth_service.go, artwork_service.go, exhibition_service.go, artist_service.go, interaction_service.go, review_service.go, audit_log_service.go
    ├── handler/                 # 按实体分文件：auth_handler.go, artwork_handler.go, exhibition_handler.go, artist_handler.go, interaction_handler.go, review_handler.go, audit_handler.go
    ├── router/                  # router.go + 按实体分文件：artworks.go, exhibitions.go, artists.go, interactions.go, reviews.go, audit.go
    ├── middleware/              # auth.go, rbac.go, audit_logger.go, content_review.go, error_handler.go, request_logger.go
    ├── dto/                     # 请求/响应结构体（按实体分文件）
    ├── constants/               # artwork.go, exhibition.go, interaction.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, app_error.go, formatters.go, uploader.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 20 条模板，如 `ARTWORK_PUBLISH_SUCCESS`、`INTERACTION_CREATE_FAILED`
   - 任何业务字段变更（如 Artwork 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名和字段名，如 `"Artwork[id=123] publish failed: review not approved"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、尺寸格式化、媒介类型文本映射、互动计数格式化
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **全局配置必须通过多个文件读取**
   - `internal/config/config.go` 读取环境变量，但 handler/service 中也可以直接读取 `os.Getenv`
   - 任何新增配置项都需要在 config、.env.example、docker-compose.yml、util 中同步出现

5. **枚举必须前后端重复定义且被多处引用**
   - ArtworkStatus、Medium、ExhibitionStatus、InteractionType 必须在前端 constants、后端 constants、类型定义、日志模板、错误码中同时存在
   - 新增一个枚举值时，必须修改至少 8 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给作品新增一个媒介类型 'AIArt'"，必须触达：Artwork 实体、前后端 constants、类型定义、日志模板、错误码、formatters、mediumOptions、上传作品表单、筛选器等 ≥ 8 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落连接逻辑。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=artvault`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: artvault`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-artvault}` 前缀
- MongoDB 数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- MongoDB 服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`（提供 `GET /healthz` 健康检查接口），前端 `depends_on` 后端健康
- 暴露端口：前端 `${FRONTEND_PORT:-18808}:80`，后端 `${BACKEND_PORT:-19308}:8080`，数据库 `27017:27017`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=artvault`
- MongoDB：`MONGO_INITDB_ROOT_USERNAME=artvault_root`、`MONGO_INITDB_ROOT_PASSWORD=artvault_pwd`、`MONGO_DB_NAME=artvault`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=18808`、`BACKEND_PORT=19308`、`DB_PORT=27017`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

后端构建要求：
- 依赖安装使用 `go mod tidy`
- 本地运行使用 `go run ./cmd/server`
- 构建使用 `go build ./...`（单服务）或 `go build ./cmd/server`
- `go.mod` / `go.sum` 必须声明全部 Go 依赖
- 后端提供 `GET /healthz` 健康检查接口，返回 200

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
coverage.out
frontend/coverage/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址（http://localhost:18808）
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + MongoDB（mongo-driver）`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`，构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 ArtworkStatus、Medium、ExhibitionStatus、InteractionType 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:18808` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<ArtworkCard>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
